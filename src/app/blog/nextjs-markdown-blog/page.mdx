
# NextJS + MDX でブログメディア構築

今日は NextJS + MDX でブログメディアを構築する方法を紹介します。
NextJSのバージョンは 2024年7月時点で最新の 14.0.0 を想定した記事となっております。

それでは早速始めていきましょう。


## 1. NextJS のインストール

## 2. MDX のインストール




- One
- Two
- Three

Checkout my React component:

import BlogLayout from '@components/BlogLayout'
export default function MDXPage({ children }) {
  return <BlogLayout>{children}</BlogLayout>
}
